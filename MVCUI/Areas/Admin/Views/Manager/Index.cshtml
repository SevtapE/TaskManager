@model List<Entities.Concrete.Manager>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}


<div class="col-sm-12">
    <section class="panel">
        <header class="panel-heading no-border ">
            <strong>Managers</strong>
        </header>
        <br />
        <table class="table table-bordered">
            <thead>
                <tr>

                    <th>Id</th>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>EmployeeRegistrationNumber</th>
                     <th>Company Name</th>
                    <th>Title</th>
                    <th>Email</th>
                    <th>Status</th>


                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <th>@item.Id</th>
                        <td>@item.User.FirstName</td>
                        <td>@item.User.LastName</td>
                        <td>@item.EmployeeRegistrationNumber</td>
                        <td>@item.Company.CompanyName</td>
                        <td>@item.Title</td>
                        <td>@item.User.Email</td>
                        <td>@item.User.Status</td>

                        <td><a href="/Admin/Manager/DeleteManager/@item.Id" class="btn btn-danger">Delete</a> </td>
                        <td><a href="/Admin/Manager/UpdateManager/@item.Id" class="btn btn-warning">Update</a> </td>


                    </tr>
                }

            </tbody>
        </table>

    </section>
    <a href="/Admin/Manager/AddManager/" class="btn btn-success">Add New</a>
</div>

